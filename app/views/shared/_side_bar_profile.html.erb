<%# サイドバーコンテンツ %>
<div class="side-bar">
 <div class="side-contents">
  <div class="info">
   メニュー
  </div>
  
   <% if user_signed_in? %>
      <div class="block">
       <%= link_to "マイページ", user_path(current_user.id), method: :get, class:"my-menu"  %>
      </div>
       <div class="block">
        <%= link_to "プロフィール",  users_path(current_user.id), method: :get, class:"my-menu"  %>
       </div>
        <div class="block">
         <%= link_to "記事を書く", new_blog_path, class:"new-blog" %>
        </div>
        <div class="block">
         <%= link_to "チャット", rooms_path(current_user.id), class:"my-menu" %>
        </div>
   <% else %>
    <div class="block">
     <%= link_to "新規登録",new_user_registration_path, class:"new-user"%>
    </div>
     <div class="block">
      <%= link_to "ログイン",user_session_path, class:"log-in" %>
     </div>
   <% end %>
    <div class="block">
     <%= link_to "一覧へ戻る", root_path, class:"root-index"%>
   </div>
  
 <div class="nav-content">
   <%= render "shared/side_search" %>
 </div>
</div>
  <%# 下段UI %>
  <div class="info2">
   ユーザー編集
  </div>
   <div class="block">
     <%= link_to "プロフィール編集", edit_user_registration_path(current_user), method: :get, local: true, class:"my-menu2" %>
   </div>
   <%# <div class="block"> %>
     <%#= link_to "パスワード変更", new_user_password_path, method: :put, local: true, class:"my-menu2" %>
   <%# </div> %>
 </div>
</div>